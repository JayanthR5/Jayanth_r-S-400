version: '3.8'
services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app/backend
      - ./static:/app/static
      - ./events_data:/app/backend/events_data
    environment:
      - FLASK_ENV=development
      - FLASK_SECRET_KEY=dev-secret-key
